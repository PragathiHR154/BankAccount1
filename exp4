git clone https://github.com/YOUR-USERNAME/exp4.git
cd exp4
git checkout -b feature_branch
touch new.txt
git add new.txt
git commit -m "Added new file"
git push origin feature_branch
(check in the github click compare and pull command and also Click Create Pull RequestReview changes Click Merge Pull Request Confirm merge)
git checkout main
git pull origin main
